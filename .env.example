# product_launch_generator 必要な環境変数の雛形
# 実運用時は `.env` にコピーして値を入れる（.env は git 管理外）
# Streamlit Cloud では App Settings > Secrets に同じキーで設定
# 現在のコードは Streamlit UI からの入力が第一経路。以下は将来的な env 化用

# Anthropic API キー（Claude 呼び出し）
ANTHROPIC_API_KEY=

# Tavily API キー（トレンド／未来リスク検索）
TAVILY_API_KEY=
